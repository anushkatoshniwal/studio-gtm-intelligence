# Comma-separated browser origins allowed to call FastAPI directly.
FRONTEND_ORIGINS=http://127.0.0.1:3000

# Use an absolute path on the Render persistent disk in production.
DATABASE_PATH=./studio_gtm.db
